@model Appli_gestion_cla.Models.Affectation

@{
    ViewData["Title"] = "Affectation";
}


<h4>Affectation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post">
           
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EnseignantId" class="control-label"></label>
                <select asp-for="EnseignantId" class="form-control" asp-items="ViewBag.EnseignantId"><option value=""> Selectionner un enseignant</option></select>
                
            </div>
            <div class="form-group">
                <label asp-for="MatiereId" class="control-label"></label>
                <select asp-for="MatiereId" class="form-control" asp-items="ViewBag.MatiereId"><option value=""> Selectionner une matiere </option></select>
                
            </div>
            <div class="form-group">
                <label asp-for="ClasseId" class="control-label"></label>
                <select asp-for="ClasseId" class="form-control" asp-items="ViewBag.ClasseId"><option value=""> Selectionner une classe </option></select>
                
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
